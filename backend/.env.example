# ===========================================
# Backend Environment Variables
# ===========================================
# Copy this file to .env and update values for your deployment

# REQUIRED: JWT Security (MUST change in production)
Auth__JwtKey=your-super-secret-jwt-key-minimum-32-characters-long-replace-this

# REQUIRED: Legacy password support (if you have existing users)
Auth__Salt=your-legacy-salt-value-DefaultSalt123!

# REQUIRED: Frontend CORS Origins (update with your actual frontend URL)
Frontend__AllowedOrigins__0=https://your-frontend-domain.com
Frontend__AllowedOrigins__1=https://www.your-frontend-domain.com

GarminOAuth__ConsumerKey=""
GarminOAuth__ConsumerSecret=""
GarminOAuth__CallbackUrl=""
GarminOAuth__RedirectUrl=""

# Database (default should work for most deployments)
ConnectionStrings__DefaultConnection=Data Source=/app/data/garmin_challenge.db

# ASP.NET Core Configuration
ASPNETCORE_ENVIRONMENT=Production
DOTNET_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:8080

# .NET Runtime Optimization
DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=false
DOTNET_EnableDiagnostics=0

# Logging Levels
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft_AspNetCore=Warning

# ===========================================
# Deployment Platform Specific
# ===========================================

# Railway/Platform Volume Mount
# Mount volume at: /app/data

# Port Configuration
# Platform will set $PORT automatically